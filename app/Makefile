# The application the kernel links when nothing else is asked for: the
# placeholder osv_app_main() in app.cc. The top-level Makefile picks the
# fragment; see the "app" comment there for the other applications.
app-objects = app/app.o
